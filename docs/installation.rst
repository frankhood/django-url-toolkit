============
Installation
============

At the command line::

    $ easy_install django-url-toolkit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-url-toolkit
    $ pip install django-url-toolkit
